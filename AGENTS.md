# Anti Survivors AI Agent Guidelines & Project Rules (`AGENTS.md`)

본 문서는 **Anti Survivors(안티 서바이버즈) 저장소를 다루는 모든 AI 어시스턴트(Antigravity, Cursor, Claude Code, GitHub Copilot, Codex 등)**가 공통으로 준수해야 하는 저장소 표준 작업 규칙입니다.  
이 저장소를 클론하거나 작업을 수행하는 모든 AI 에이전트는 아래 지침을 반드시 숙지하고 작업을 수행해야 합니다.

---

## 1. 프로젝트 네비게이션 및 아키텍처 원칙

1. **`main.md` 사전 참조 필수**:
   - 코드 검색이나 파일 수정 전, 반드시 저장소 루트의 [main.md](main.md)를 먼저 확인하여 디렉토리 구조, 모듈별 역할, 클래스/함수 인터페이스를 파악합니다.
   - 전체 코드베이스를 무차별적으로 검색하지 않고, [main.md](main.md)의 **'빠른 참조 맵(Fast Lookup)'**을 활용해 변경할 타겟 파일만 특정합니다.
2. **구조 동기화 의무 (Maintenance Rules)**:
   - 새로운 모듈, 무기, 몬스터, 카드, UI 컴포넌트를 추가하거나 기존 인터페이스를 대폭 변경한 경우, 반드시 [main.md](main.md)의 디렉토리 맵과 명세표에도 동일하게 반영해야 합니다.
3. **AI 작업 표준 워크플로우**:
   ```text
   작업 지시 수신 ➔ main.md 읽기 ➔ 작업 수행 (최소 침습적 수정) ➔ main.md 업데이트 ➔ (사용자 요청 시에만) README.md 및 docs/ 갱신
   ```

---

## 2. 기술 스택 및 환경 관리 규칙

1. **순수 바닐라 JS 및 웹 표준 준수**:
   - 본 프로젝트는 외부 프레임워크(React, Vue 등)나 무거운 번들러 없이 **HTML5 Canvas, Pure Vanilla JavaScript (ES6+), Web Audio API**로 구현된 초경량 네이티브 게임 엔진입니다.
   - 불필요한 외부 라이브러리 추가를 금지하며, 웹 표준 Canvas 2D 렌더링 파이프라인을 존중합니다.
2. **패키지 및 도구 설치 사전 승인**:
   - 패키지, 라이브러리, CLI 도구 설치가 필요한 경우 임의로 실행하지 않고, 용도와 필요성을 설명한 뒤 사용자의 명시적 허락을 받고 진행합니다.
3. **로컬 서버 구동 환경**:
   - 로컬 테스트 서버는 Node.js 내장 모듈(`http`, `fs`, `path`)만으로 작동하는 [`server.js`](server.js)를 사용합니다.

---

## 3. 핵심 게임 시스템 및 설계 불변 원칙

1. **세이브 데이터 무결성 보호**:
   - 로컬 스토리지에 저장되는 유저 재화(Gold) 및 영구 강화 스탯은 **FNV-1a 32비트 해시 체크섬**으로 변조를 방지합니다 ([`js/saveManager.js`](js/saveManager.js)).
   - 세이브 데이터 구조를 변경할 때는 체크섬 계산 함수와의 정합성을 반드시 검증해야 합니다.
2. **전투 밸런스 및 슬롯 규칙**:
   - 무기 장착 한도는 최대 **6개**입니다.
   - 모든 기본 및 진화 무기의 강화 상한은 **5레벨 (MAX)**입니다.
   - 원거리 투사체 몬스터(`cultist`, `darkMage`)는 스폰 생성량이 2/3 감축 밸런스를 유지해야 합니다.
   - 보스는 필드 폭탄 습득 시 즉사하지 않고 최대 체력의 10% 또는 최대 250 데미지로 상한이 제한됩니다.

---

## 4. 코딩 4대 원칙 (The 4 Core Principles of Coding)

1. **Think Before Coding (사전 생각)**:
   - 코드 작성 전 가정사항과 발생 가능한 예외를 먼저 정리하고, 트레이드오프를 검토합니다.
2. **Simplicity First (단순성 우선)**:
   - 불필요한 추상화나 제네릭 래퍼를 피하고, 직관적이며 가장 이해하기 쉬운 코드를 작성합니다.
3. **Surgical Changes (최소 침습적 수정)**:
   - 작업 완료에 꼭 필요한 라인, 함수, 모듈만 정확히 수정하며 인접 코드의 포맷팅을 보존합니다.
4. **Goal-Driven Execution (목표 중심 실행)**:
   - 수정 후 구문 오류 검사(`node -c ...`) 및 브라우저 호환성을 철저히 검증합니다.

---

## 5. 버전 관리 및 푸시 통제

1. **Git 원자적 커밋 (Atomic Commits)**:
   - 커밋은 성격별(`feat:`, `fix:`, `docs:`, `balance:`, `refactor:`, `chore:`)로 분리하여 한글로 명확하게 작성합니다.
2. **Git Push 임의 실행 절대 금지**:
   - 원격 저장소(`git push`) 푸시는 사용자가 직접 명시적으로 요청한 경우에만 수행합니다.
